---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: cluster-ci-cache
labels:
  - pairs:
      app.kubernetes.io/name: cluster-ci-cache
    includeSelectors: true
resources:
  - config.yaml
  - rbac.yaml
  - server.yaml
  - agent.yaml
